//! Polars-independent interval algorithms.
//!
//! This crate will contain interval algorithms implemented in Rust, independent
//! of Polars and its data types. No interval algorithms are implemented yet.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke_test() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
